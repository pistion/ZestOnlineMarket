const legacy = require("./page.controller.legacy");

module.exports = {
  renderCartPage: legacy.renderCartPage,
  renderCheckoutPage: legacy.renderCheckoutPage,
  renderCompatibilityProductViewer: legacy.renderCompatibilityProductViewer,
};
